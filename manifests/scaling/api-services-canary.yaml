apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: workshop
  labels:
    app: api
spec:
  selector:
    app: api
  ports:
  - port: 80
    targetPort: http
    name: http
---
apiVersion: v1
kind: Service
metadata:
  name: api-canary
  namespace: workshop
  labels:
    app: api
spec:
  selector:
    app: api
  ports:
  - port: 80
    targetPort: http
    name: http

